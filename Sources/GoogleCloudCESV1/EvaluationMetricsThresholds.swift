// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// Threshold settings for metrics in an Evaluation.
public struct EvaluationMetricsThresholds: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Optional. The golden evaluation metrics thresholds.
  public var goldenEvaluationMetricsThresholds:
    EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds? = nil

  /// Optional. Deprecated: Use `golden_hallucination_metric_behavior` instead.
  /// The hallucination metric behavior is currently used for golden evaluations.
  @available(*, deprecated)
  public var hallucinationMetricBehavior: EvaluationMetricsThresholds.HallucinationMetricBehavior =
    EvaluationMetricsThresholds.HallucinationMetricBehavior()

  /// Optional. The hallucination metric behavior for golden evaluations.
  public var goldenHallucinationMetricBehavior:
    EvaluationMetricsThresholds.HallucinationMetricBehavior =
      EvaluationMetricsThresholds.HallucinationMetricBehavior()

  /// Optional. The hallucination metric behavior for scenario evaluations.
  public var scenarioHallucinationMetricBehavior:
    EvaluationMetricsThresholds.HallucinationMetricBehavior =
      EvaluationMetricsThresholds.HallucinationMetricBehavior()

  /// Initialize a new instance of `EvaluationMetricsThresholds`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = EvaluationMetricsThresholds().with { $0.goldenEvaluationMetricsThresholds = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Settings for golden evaluations.
  public struct GoldenEvaluationMetricsThresholds: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The turn level metrics thresholds.
    public var turnLevelMetricsThresholds:
      EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds.TurnLevelMetricsThresholds? =
        nil

    /// Optional. The expectation level metrics thresholds.
    public var expectationLevelMetricsThresholds:
      EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds
        .ExpectationLevelMetricsThresholds? = nil

    /// Optional. The tool matching settings. An
    /// extra tool call is a tool call that is present in the execution but does
    /// not match any tool call in the golden expectation.
    public var toolMatchingSettings: EvaluationMetricsThresholds.ToolMatchingSettings? = nil

    /// Initialize a new instance of `GoldenEvaluationMetricsThresholds`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GoldenEvaluationMetricsThresholds().with { $0.turnLevelMetricsThresholds = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Turn level metrics thresholds.
    public struct TurnLevelMetricsThresholds: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. The success threshold for semantic similarity. Must be an
      /// integer between 0 and 4. Default is >= 3.
      public var semanticSimilaritySuccessThreshold: Swift.Int32? = nil

      /// Optional. The success threshold for overall tool invocation
      /// correctness. Must be a float between 0 and 1. Default is 1.0.
      public var overallToolInvocationCorrectnessThreshold: Swift.Float? = nil

      /// Optional. The semantic similarity channel to use for evaluation.
      public var semanticSimilarityChannel:
        EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds.TurnLevelMetricsThresholds
          .SemanticSimilarityChannel = EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds
          .TurnLevelMetricsThresholds.SemanticSimilarityChannel()

      /// Initialize a new instance of `TurnLevelMetricsThresholds`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = TurnLevelMetricsThresholds().with { $0.semanticSimilaritySuccessThreshold = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Semantic similarity channel to use.
      public enum SemanticSimilarityChannel: Codable, Equatable, Sendable {
        /// Metric unspecified. Defaults to TEXT.
        case unspecified
        /// Use text semantic similarity.
        case text
        /// Use audio semantic similarity.
        case audio
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .text: return 1
          case .audio: return 2
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "SEMANTIC_SIMILARITY_CHANNEL_UNSPECIFIED"
          case .text: return "TEXT"
          case .audio: return "AUDIO"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SemanticSimilarityChannel/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "SEMANTIC_SIMILARITY_CHANNEL_UNSPECIFIED": self = .unspecified
          case "TEXT": self = .text
          case "AUDIO": self = .audio
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SemanticSimilarityChannel/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .text
          case 2: self = .audio
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .text: return try container.encode(1)
          case .audio: return try container.encode(2)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.ces.v1.EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds.TurnLevelMetricsThresholds"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Expectation level metrics thresholds.
    public struct ExpectationLevelMetricsThresholds: Codable, Equatable, GoogleCloudWkt
        ._AnyPackable,
      Sendable
    {
      /// Optional. The success threshold for individual tool invocation
      /// parameter correctness. Must be a float between 0 and 1. Default is 1.0.
      public var toolInvocationParameterCorrectnessThreshold: Swift.Float? = nil

      /// Initialize a new instance of `ExpectationLevelMetricsThresholds`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ExpectationLevelMetricsThresholds().with { $0.toolInvocationParameterCorrectnessThreshold = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.ces.v1.EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds.ExpectationLevelMetricsThresholds"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.ces.v1.EvaluationMetricsThresholds.GoldenEvaluationMetricsThresholds"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Settings for matching tool calls.
  public struct ToolMatchingSettings: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Behavior for extra tool calls.
    /// Defaults to FAIL.
    public var extraToolCallBehavior:
      EvaluationMetricsThresholds.ToolMatchingSettings.ExtraToolCallBehavior =
        EvaluationMetricsThresholds.ToolMatchingSettings.ExtraToolCallBehavior()

    /// Initialize a new instance of `ToolMatchingSettings`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ToolMatchingSettings().with { $0.extraToolCallBehavior = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Defines the behavior when an extra tool call is encountered. An extra
    /// tool call is a tool call that is present in the execution but does not
    /// match any tool call in the golden expectation.
    public enum ExtraToolCallBehavior: Codable, Equatable, Sendable {
      /// Unspecified behavior. Defaults to FAIL.
      case unspecified
      /// Fail the evaluation if an extra tool call is encountered.
      case fail
      /// Allow the extra tool call.
      case allow
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .fail: return 1
        case .allow: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "EXTRA_TOOL_CALL_BEHAVIOR_UNSPECIFIED"
        case .fail: return "FAIL"
        case .allow: return "ALLOW"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ExtraToolCallBehavior/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "EXTRA_TOOL_CALL_BEHAVIOR_UNSPECIFIED": self = .unspecified
        case "FAIL": self = .fail
        case "ALLOW": self = .allow
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ExtraToolCallBehavior/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .fail
        case 2: self = .allow
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .fail: return try container.encode(1)
        case .allow: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.ces.v1.EvaluationMetricsThresholds.ToolMatchingSettings"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The hallucination metric behavior. Regardless of the behavior, the metric
  /// will always be calculated. The difference is that when disabled, the
  /// metric is not used to calculate the overall evaluation score.
  public enum HallucinationMetricBehavior: Codable, Equatable, Sendable {
    /// Unspecified hallucination metric behavior.
    case unspecified
    /// Disable hallucination metric.
    case disabled
    /// Enable hallucination metric.
    case enabled
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .disabled: return 1
      case .enabled: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "HALLUCINATION_METRIC_BEHAVIOR_UNSPECIFIED"
      case .disabled: return "DISABLED"
      case .enabled: return "ENABLED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:HallucinationMetricBehavior/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "HALLUCINATION_METRIC_BEHAVIOR_UNSPECIFIED": self = .unspecified
      case "DISABLED": self = .disabled
      case "ENABLED": self = .enabled
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:HallucinationMetricBehavior/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .disabled
      case 2: self = .enabled
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .disabled: return try container.encode(1)
      case .enabled: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.ces.v1.EvaluationMetricsThresholds"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
