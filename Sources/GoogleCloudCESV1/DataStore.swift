// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) public import GoogleWKT

/// A DataStore resource in Vertex AI Search.
public struct DataStore: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Required. Full resource name of the DataStore.
  /// Format:
  /// `projects/{project}/locations/{location}/collections/{collection}/dataStores/{dataStore}`
  public var name: Swift.String = Swift.String()

  /// Output only. The type of the data store. This field is readonly and
  /// populated by the server.
  public var type: DataStore.DataStoreType = DataStore.DataStoreType()

  /// Output only. The document processing mode for the data store connection.
  /// Only set for PUBLIC_WEB and UNSTRUCTURED data stores.
  public var documentProcessingMode: DataStore.DocumentProcessingMode =
    DataStore.DocumentProcessingMode()

  /// Output only. The display name of the data store.
  public var displayName: Swift.String = Swift.String()

  /// Output only. Timestamp when the data store was created.
  public var createTime: GoogleWKT.Timestamp? = nil

  /// Output only. The connector config for the data store connection.
  public var connectorConfig: DataStore.ConnectorConfig? = nil

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `DataStore`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = DataStore().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let name = CodingKeys(stringValue: "name")
    static let type = CodingKeys(stringValue: "type")
    static let documentProcessingMode = CodingKeys(stringValue: "documentProcessingMode")
    static let displayName = CodingKeys(stringValue: "displayName")
    static let createTime = CodingKeys(stringValue: "createTime")
    static let connectorConfig = CodingKeys(stringValue: "connectorConfig")

    static let _knownKeys: Set<Swift.String> = [
      "name",
      "type",
      "documentProcessingMode",
      "displayName",
      "createTime",
      "connectorConfig",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
      self.name = value
    }
    if let value = try container.decodeIfPresent(DataStore.DataStoreType.self, forKey: .type) {
      self.type = value
    }
    if let value = try container.decodeIfPresent(
      DataStore.DocumentProcessingMode.self, forKey: .documentProcessingMode)
    {
      self.documentProcessingMode = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
      self.displayName = value
    }
    self.createTime = try container.decodeIfPresent(GoogleWKT.Timestamp.self, forKey: .createTime)
    self.connectorConfig = try container.decodeIfPresent(
      DataStore.ConnectorConfig.self, forKey: .connectorConfig)
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.type, forKey: .type)
    try container.encode(self.documentProcessingMode, forKey: .documentProcessingMode)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encodeIfPresent(self.createTime, forKey: .createTime)
    try container.encodeIfPresent(self.connectorConfig, forKey: .connectorConfig)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// The connector config for the data store connection.
  public struct ConnectorConfig: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Resource name of the collection the data store belongs to.
    public var collection: Swift.String = Swift.String()

    /// Display name of the collection the data store belongs to.
    public var collectionDisplayName: Swift.String = Swift.String()

    /// The name of the data source.
    /// Example: `salesforce`, `jira`, `confluence`, `bigquery`.
    public var dataSource: Swift.String = Swift.String()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `ConnectorConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ConnectorConfig().with { $0.collection = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let collection = CodingKeys(stringValue: "collection")
      static let collectionDisplayName = CodingKeys(stringValue: "collectionDisplayName")
      static let dataSource = CodingKeys(stringValue: "dataSource")

      static let _knownKeys: Set<Swift.String> = [
        "collection",
        "collectionDisplayName",
        "dataSource",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .collection) {
        self.collection = value
      }
      if let value = try container.decodeIfPresent(
        Swift.String.self, forKey: .collectionDisplayName)
      {
        self.collectionDisplayName = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .dataSource) {
        self.dataSource = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.collection, forKey: .collection)
      try container.encode(self.collectionDisplayName, forKey: .collectionDisplayName)
      try container.encode(self.dataSource, forKey: .dataSource)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.ces.v1.DataStore.ConnectorConfig"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// The type of the data store.
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum DataStoreType: Codable, Equatable, Sendable {
    /// Not specified. This value indicates that the data store type is not
    /// specified, so it will not be used during search.
    case unspecified
    /// A data store that contains public web content.
    case publicWeb
    /// A data store that contains unstructured private data.
    case unstructured
    /// A data store that contains structured data used as FAQ.
    case faq
    /// A data store that is a connector to a first-party or a third-party
    /// service.
    case connector
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .publicWeb: return 1
      case .unstructured: return 2
      case .faq: return 3
      case .connector: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "DATA_STORE_TYPE_UNSPECIFIED"
      case .publicWeb: return "PUBLIC_WEB"
      case .unstructured: return "UNSTRUCTURED"
      case .faq: return "FAQ"
      case .connector: return "CONNECTOR"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DataStoreType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "DATA_STORE_TYPE_UNSPECIFIED": self = .unspecified
      case "PUBLIC_WEB": self = .publicWeb
      case "UNSTRUCTURED": self = .unstructured
      case "FAQ": self = .faq
      case "CONNECTOR": self = .connector
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DataStoreType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .publicWeb
      case 2: self = .unstructured
      case 3: self = .faq
      case 4: self = .connector
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("DATA_STORE_TYPE_UNSPECIFIED")
      case .publicWeb: return try container.encode("PUBLIC_WEB")
      case .unstructured: return try container.encode("UNSTRUCTURED")
      case .faq: return try container.encode("FAQ")
      case .connector: return try container.encode("CONNECTOR")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The document processing mode of the data store.
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum DocumentProcessingMode: Codable, Equatable, Sendable {
    /// Not specified.
    case unspecified
    /// Documents are processed as documents.
    case documents
    /// Documents are converted to chunks.
    case chunks
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .documents: return 1
      case .chunks: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "DOCUMENT_PROCESSING_MODE_UNSPECIFIED"
      case .documents: return "DOCUMENTS"
      case .chunks: return "CHUNKS"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DocumentProcessingMode/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "DOCUMENT_PROCESSING_MODE_UNSPECIFIED": self = .unspecified
      case "DOCUMENTS": self = .documents
      case "CHUNKS": self = .chunks
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DocumentProcessingMode/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .documents
      case 2: self = .chunks
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("DOCUMENT_PROCESSING_MODE_UNSPECIFIED")
      case .documents: return try container.encode("DOCUMENTS")
      case .chunks: return try container.encode("CHUNKS")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.ces.v1.DataStore"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
