// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// A conversation represents an interaction between an end user and the CES app.
public struct Conversation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Identifier. The unique identifier of the conversation.
  /// Format:
  /// `projects/{project}/locations/{location}/apps/{app}/conversations/{conversation}`
  public var name: Swift.String = Swift.String()

  /// Output only. Timestamp when the conversation was created.
  public var startTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Timestamp when the conversation was completed.
  public var endTime: GoogleCloudWkt.Timestamp? = nil

  /// Required. The turns in the conversation.
  public var turns: [Conversation.Turn] = []

  /// Output only. The number of turns in the conversation.
  public var turnCount: Swift.Int32 = Swift.Int32()

  /// DEPRECATED. Please use
  /// [input_types][google.cloud.ces.v1.Conversation.input_types] instead.
  ///
  /// [google.cloud.ces.v1.Conversation.input_types]: <doc:Conversation/inputTypes>
  @available(*, deprecated)
  public var channelType: Conversation.ChannelType = Conversation.ChannelType()

  /// Output only. Indicate the source of the conversation.
  public var source: Conversation.Source = Conversation.Source()

  /// Output only. The input types of the conversation.
  public var inputTypes: [Conversation.InputType] = []

  /// Output only. The agent that initially handles the conversation. If not
  /// specified, the conversation is handled by the root agent. Format:
  /// `projects/{project}/locations/{location}/apps/{app}/agents/{agent}`
  public var entryAgent: Swift.String = Swift.String()

  /// Output only. The deployment of the app used for processing the
  /// conversation. Format:
  /// `projects/{project}/locations/{location}/apps/{app}/deployments/{deployment}`
  public var deployment: Swift.String = Swift.String()

  /// Output only. The version of the app used for processing the conversation.
  /// Format:
  /// `projects/{project}/locations/{location}/apps/{app}/versions/{version}`
  public var appVersion: Swift.String = Swift.String()

  /// Output only. The language code of the conversation.
  public var languageCode: Swift.String = Swift.String()

  /// Deprecated. Use turns instead.
  @available(*, deprecated)
  public var messages: [Message] = []

  /// Initialize a new instance of `Conversation`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Conversation().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// All information about a single turn in the conversation.
  public struct Turn: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. List of messages in the conversation turn, including user
    /// input, agent responses and intermediate events during the processing.
    public var messages: [Message] = []

    /// Optional. The root span of the action processing.
    public var rootSpan: Span? = nil

    /// Initialize a new instance of `Turn`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Turn().with { $0.messages = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.ces.v1.Conversation.Turn"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The channel type of the conversation.
  public enum ChannelType: Codable, Equatable, Sendable {
    /// Unspecified channel type.
    case unspecified
    /// The conversation only contains text messages between the end user and the
    /// agent.
    case text
    /// The conversation contains audio messages between the end user and the
    /// agent.
    case audio
    /// The conversation multi-modal messages (e.g. image) between the end user
    /// and the agent.
    case multimodal
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .text: return 1
      case .audio: return 2
      case .multimodal: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "CHANNEL_TYPE_UNSPECIFIED"
      case .text: return "TEXT"
      case .audio: return "AUDIO"
      case .multimodal: return "MULTIMODAL"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "CHANNEL_TYPE_UNSPECIFIED": self = .unspecified
      case "TEXT": self = .text
      case "AUDIO": self = .audio
      case "MULTIMODAL": self = .multimodal
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .text
      case 2: self = .audio
      case 3: self = .multimodal
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .text: return try container.encode(1)
      case .audio: return try container.encode(2)
      case .multimodal: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The source of the conversation.
  public enum Source: Codable, Equatable, Sendable {
    /// Unspecified source.
    case unspecified
    /// The conversation is from the live end user.
    case live
    /// The conversation is from the simulator.
    case simulator
    /// The conversation is from the evaluation.
    case eval
    /// The conversation is from an agent tool. Agent tool runs the agent in a
    /// separate session, which is persisted for testing and debugging purposes.
    case agentTool
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .live: return 1
      case .simulator: return 2
      case .eval: return 3
      case .agentTool: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "SOURCE_UNSPECIFIED"
      case .live: return "LIVE"
      case .simulator: return "SIMULATOR"
      case .eval: return "EVAL"
      case .agentTool: return "AGENT_TOOL"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "SOURCE_UNSPECIFIED": self = .unspecified
      case "LIVE": self = .live
      case "SIMULATOR": self = .simulator
      case "EVAL": self = .eval
      case "AGENT_TOOL": self = .agentTool
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .live
      case 2: self = .simulator
      case 3: self = .eval
      case 4: self = .agentTool
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .live: return try container.encode(1)
      case .simulator: return try container.encode(2)
      case .eval: return try container.encode(3)
      case .agentTool: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Type of the input message.
  public enum InputType: Codable, Equatable, Sendable {
    /// Unspecified input type.
    case unspecified
    /// Text input.
    case text
    /// Audio input.
    case audio
    /// Image input.
    case image
    /// Blob input.
    case blob
    /// Client function tool response input.
    case toolResponse
    /// Variables input.
    case variables
    /// Event input.
    case event
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .text: return 1
      case .audio: return 2
      case .image: return 3
      case .blob: return 4
      case .toolResponse: return 5
      case .variables: return 6
      case .event: return 7
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "INPUT_TYPE_UNSPECIFIED"
      case .text: return "INPUT_TYPE_TEXT"
      case .audio: return "INPUT_TYPE_AUDIO"
      case .image: return "INPUT_TYPE_IMAGE"
      case .blob: return "INPUT_TYPE_BLOB"
      case .toolResponse: return "INPUT_TYPE_TOOL_RESPONSE"
      case .variables: return "INPUT_TYPE_VARIABLES"
      case .event: return "INPUT_TYPE_EVENT"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "INPUT_TYPE_UNSPECIFIED": self = .unspecified
      case "INPUT_TYPE_TEXT": self = .text
      case "INPUT_TYPE_EVENT": self = .event
      case "INPUT_TYPE_AUDIO": self = .audio
      case "INPUT_TYPE_IMAGE": self = .image
      case "INPUT_TYPE_BLOB": self = .blob
      case "INPUT_TYPE_TOOL_RESPONSE": self = .toolResponse
      case "INPUT_TYPE_VARIABLES": self = .variables
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .text
      case 2: self = .audio
      case 3: self = .image
      case 4: self = .blob
      case 5: self = .toolResponse
      case 6: self = .variables
      case 7: self = .event
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .text: return try container.encode(1)
      case .audio: return try container.encode(2)
      case .image: return try container.encode(3)
      case .blob: return try container.encode(4)
      case .toolResponse: return try container.encode(5)
      case .variables: return try container.encode(6)
      case .event: return try container.encode(7)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.ces.v1.Conversation"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
